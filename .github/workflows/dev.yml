name: Deploy Web

on:
  push:
    branches:
      - main

jobs:
  echo-private-key-to-file:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.PRKEY }}
        run: |
          echo "$SUPER_SECRET"
